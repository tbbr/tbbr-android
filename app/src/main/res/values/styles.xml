<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/secondaryBase</item>
        <item name="colorPrimaryDark">@color/secondaryDark</item>
        <item name="colorAccent">@color/primaryBase</item>

        <item name="android:colorBackground">@color/secondaryDark</item>
        <item name="android:textColor">@color/grey400</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
